# Cybersecurity page consolidation — 2026-08-24

Consolidated the two published Cyber Security pages around the Huntress
partnership page at `/cybersecurity-dublin/` (page `989829`).

- legacy page `988509` was changed from published to draft;
- Services-menu item `988549` was repointed to the canonical Huntress page;
- exact internal references to the legacy URL were replaced;
- the legacy URL remains a direct redirect to the canonical page;
- the legacy URL was removed from the page sitemap while the canonical URL
  remains indexed.

A timestamped rollback snapshot captured the legacy page, Elementor data, menu
metadata and updated-reference inventory before consolidation.
